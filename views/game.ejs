<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Juste Prix – Partie</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

<div class="container py-5">
  <h1 class="text-center mb-4">LE JEU DU JUSTE PRIX</h1>

  <div class="card shadow mx-auto" style="max-width: 540px;">
    <div class="card-body">

      <!-- Infos objet + joueur -->
      <p class="fs-5">
        <strong><%= prenom %></strong>, devine le prix de :  
        <span class="fw-semibold"><%= objet %></span>
      </p>

      <!-- Dernière tentative + message -->
            <% if (msg) { %>
                <div class="alert <%= gagne ? 'alert-success' : 'alert-warning' %> text-center">
                  <% if (!gagne) { %>
                    Tu as proposé : <strong><%= proposition || '« vide »' %></strong><br>
                  <% } %>
                  <%= msg %>
                  <% if (gagne) { %>
                    après <strong><%= essais %></strong> tentative(s).
                  <% } %>
                </div>
              <% } %>
        

      <% if (!gagne) { %>
        <!-- Formulaire de tentative -->
        <form action="/game" method="post" class="d-flex gap-2">
          <input type="text" name="proposition" class="form-control"
                 placeholder="Tente ta chance !" autofocus>
          <button type="submit" class="btn btn-dark">Valider</button>
        </form>
        <p class="mt-3 mb-0 text-muted text-end">Tentatives : <%= essais %></p>
      <% } else { %>
        <!-- Bouton Rejouer -->
        <div class="d-grid">
          <a href="/replay" class="btn btn-dark btn-lg">Rejouer</a>
        </div>
      <% } %>

    </div>
  </div>
</div>

</body>
</html>
